{
  "name": "@jam/core",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./jsx": "./src/jsx.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench",
    "bench:ci": "mkdir -p benchmark-results && vitest bench --outputJson benchmark-results/core-benchmarks.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jam/engine": "workspace:*"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^17.6.3",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
