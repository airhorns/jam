{
  "name": "@jam/puddy-vite",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint -c .oxlintrc.json src",
    "test": "oxlint -c .oxlintrc.json src && vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:record": "tsx src/testing/record.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jam/core": "workspace:*",
    "@jam/ui": "workspace:*",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@sandbox-agent/cli": "0.4.2",
    "msw": "^2.12.14",
    "oxlint": "^1.60.0",
    "tsx": "^4.8.1",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
